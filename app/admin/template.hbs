<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to "admin" class="navbar-brand"}}BC Jobs Administration{{/link-to}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#link-to "admin.rules" tagName="li"}}
          {{#link-to "admin.rules"}}
            Normalization rules
          {{/link-to}}
        {{/link-to}}
        {{#link-to "admin.users" tagName="li"}}
          {{#link-to "admin.users"}}
            Users
          {{/link-to}}
        {{/link-to}}
        {{#link-to "admin.export" tagName="li"}}
          {{#link-to "admin.export"}}
            Export
          {{/link-to}}
        {{/link-to}}
        {{#link-to "admin.accounts" tagName="li"}}
          {{#link-to "admin.accounts"}}
            Accounts
          {{/link-to}}
        {{/link-to}}
        {{#link-to "admin.imports" tagName="li"}}
          {{#link-to "admin.imports"}}
            Daily Imports
          {{/link-to}}
        {{/link-to}}
      </ul>

      <ul class="nav navbar-nav navbar-right">
        {{#link-to "admin.users.edit" model tagName="li"}}
          {{#link-to "admin.users.edit" model}}
            {{model.firstName}} {{model.lastName}}
          {{/link-to}}
        {{/link-to}}
        <li><a href="#" {{action "logout"}}>Logout</a></li>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

{{outlet}}
